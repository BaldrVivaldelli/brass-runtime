/** @internal HTTP compatibility name for the runtime's direct adapter runner. */
export {
  registerEffectDirect as registerHttpEffect,
  type EffectCanceler,
} from "../core/runtime/directEffectRunner";

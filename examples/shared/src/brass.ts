// Keep the v2 preview move reversible for every framework example.
export { Layer, LayerContext, Runtime } from "brass-runtime/next";

// Evidence-backed preview gaps used by the Layer/DI bootstrap.
export { RuntimeService, makeConfigLayer } from "brass-runtime/core";
